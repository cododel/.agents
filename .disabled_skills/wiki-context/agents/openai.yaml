interface:
  display_name: "Wiki Context"
  short_description: "Retrieve auxiliary Wiki context when repository evidence needs it."
  default_prompt: "Use $wiki-context only if this task has a material historical or cross-project context gap."
