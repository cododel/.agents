interface:
  display_name: "Contract Writer"
  short_description: "Maintains lightweight stable-boundary contracts"
  default_prompt: "Use $contract-writer to align the stable boundary contract with established behavior."
policy:
  allow_implicit_invocation: true
