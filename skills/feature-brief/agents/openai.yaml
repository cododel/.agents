interface:
  display_name: "Feature Brief"
  short_description: "Runs focused requirements briefings for large features"
  default_prompt: "Use $feature-brief to resolve material feature decisions in native plan mode."
policy:
  allow_implicit_invocation: true
