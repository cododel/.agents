interface:
  display_name: "Feature Brief"
  short_description: "Runs focused requirements briefings for large features"
  default_prompt: "Use $feature-brief to resolve material feature decisions through a focused requirements briefing."
policy:
  allow_implicit_invocation: true
