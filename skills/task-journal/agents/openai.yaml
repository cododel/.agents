interface:
  display_name: "Task Journal"
  short_description: "Preserves task state across compaction and handoffs"
  default_prompt: "Use $task-journal to preserve the current long-running task contract and state."
policy:
  allow_implicit_invocation: true
