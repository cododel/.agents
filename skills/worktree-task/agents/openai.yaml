interface:
  display_name: "Worktree Task"
  short_description: "Prepares isolated Git worktrees with MCP readiness"
  default_prompt: "Use $worktree-task to prepare an isolated branch and worktree for this task."
policy:
  allow_implicit_invocation: true
